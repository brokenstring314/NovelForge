appId: com.novelforge.app
productName: NovelForge
copyright: © 2025 NovelForge
directories:
  buildResources: build
icon: build/icon.ico
beforePack: ./scripts/prepare-windows-electron.cjs
afterAllArtifactBuild: ./scripts/cleanup-windows-electron.cjs
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!dist-web/**'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
asarUnpack:
  - backend/**
# 将后端放到与可执行文件同级
extraFiles:
  - from: ../backend/dist
    to: backend
  - from: ../backend/.env.example
    to: backend/.env

win:
  executableName: NovelForge
  publisherName: NovelForge Team
  signAndEditExecutable: true
  target:
    - dir
  icon: build/icon.ico
extraMetadata:
  CompanyName: NovelForge Team
  FileDescription: NovelForge
  ProductName: NovelForge
  OriginalFilename: NovelForge.exe
# 移除 NSIS 安装器相关配置（仅保留便携版）
mac:
  icon: build/icon.icns
  entitlements: resources/entitlements.mac.plist
  entitlementsInherit: resources/entitlements.mac.plist
  # 无开发者证书：跳过 electron-builder 的签名，改由 CI 在打包后做 ad-hoc 签名
  identity: null
  hardenedRuntime: false
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: false
dmg:
  artifactName: ${name}-${version}.${ext}
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: electronjs.org
  category: Utility
appImage:
  artifactName: ${name}-${version}.${ext}
npmRebuild: false
publish:
  provider: generic
  url: https://example.com/auto-updates
electronDownload:
  mirror: https://npmmirror.com/mirrors/electron/
